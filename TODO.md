# TODO

- Use an inverse color ring around the brush and eraser previews so it's always visible
- Make dropper preview a zoomed in version of where the cursor is above the cursor and also update the hex code as it's moving
- Brush size should scale with canvas size, so assume the canvas is 400x300, if brush size is 100, then the brush should be 1/3 of the entire height

- Add zoom and panning to drawing canvas, scroll=zoom, middle/right click drag=pan, ctrl+zoom=scroll up/down, shift+zoom=scroll left/right, also add a button in the corner of the canvas to reset view

- Make floating divs
